fish abbreviation list:
```
abbr -a -g -- cp 'cp -i'
abbr -a -U -- dotfiles '/usr/local/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'
abbr -a -U -- fish-profile 'vim ~/.config/fish/config.fish'
abbr -a -U -- g git
abbr -a -U -- ga 'git add .'
abbr -a -U -- gb 'git checkout -b'
abbr -a -U -- gcam 'git commit -am'
abbr -a -U -- gcl 'git clone'
abbr -a -U -- gcm 'git commit -m'
abbr -a -U -- gco 'git checkout'
abbr -a -U -- gf 'git fetch'
abbr -a -U -- gm 'git merge'
abbr -a -U -- gps 'git push'
abbr -a -U -- gpu 'git pull'
abbr -a -U -- gs 'git status'
abbr -a -U -- latex 'latexmk -pdf -pvc'
abbr -a -g -- mkdir 'mkdir -p'
abbr -a -g -- mongo-start 'brew services start mongodb-community'
abbr -a -g -- mongo-stop 'brew services stop mongodb-community'
abbr -a -g -- mv 'mv -i'
abbr -a -g -- myclangpp 'clang++ -Wall -std=c++11'
abbr -a -g -- myclangpp-openmp 'myclangpp -Xpreprocessor -fopenmp -lomp'
abbr -a -g -- mygcc 'gcc -Wall -pedantic -std=c11'
abbr -a -U -- py python3
abbr -a -g -- rm 'rm -i'
abbr -a -U -- ssh-disc 'ssh -K f0044rv@discovery'
abbr -a -g -- ssh-discovery 'ssh -K f0044rv@discovery'
abbr -a -g -- ssh-flume 'ssh jeffzyliu@flume.cs.dartmouth.edu'
abbr -a -g -- ssh-moose 'ssh jeffzyliu@moose.cs.dartmouth.edu'
abbr -a -U -- starship-profile 'vim ~/.config/starship.toml'
abbr -a -U -- v vim
abbr -a -U -- vrc 'vim ~/.vimrc'
abbr -a -U -- y yarn
```
more notes to be added as necessary
